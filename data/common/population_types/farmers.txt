farmers = {
	color = { 198 12 48 }
	icon_tag = slaves
	equivalent_types += laborers
	equivalent_types += tribesmen

	holding_types += village
	holding_types += town
	holding_types += city
	holding_types += metropolis
	holding_types += manor
	holding_types += monastery
}
