laborers = { #unskilled laborers who work in mining and the like
	color = { 160 160 160 }

	holding_types += village
	holding_types += town
	holding_types += city
	holding_types += metropolis
	holding_types += manor
	holding_types += monastery
}
